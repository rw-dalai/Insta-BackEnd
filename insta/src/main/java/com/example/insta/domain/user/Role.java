package com.example.insta.domain.user;

// Used to authorize users in Spring Security.
// Use Roles that make sense for your application.
// Role based authorization is a common approach.
public enum Role {
  ADMIN,
  USER
}
